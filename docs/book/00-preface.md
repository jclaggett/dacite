# Preface

Dacite: distributed immutable data with content-addressed nodes.

## Chapters (Five Layers)

1. **Stores** — content-addressed map, root ref, layers, sync.
2. **Hash Fusion** — fuse primitive and content identity.
3. **Values** — scalar/seq/map + types.
4. **Authorized Stores** — PoP, GET/PUT, auth stores.
5. **Sharing** — shares map, claim protocol.

Stop after 1 (persistence), 2 (hashing), 3 (structs), 4 (auth stores), 5 (multi-user).

## Reading

Sequential; SPEC.md for precision.

## For

Users, implementors, designers.

*Jonathan & Gorm, 2026*
