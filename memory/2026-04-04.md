# 2026-04-04

## Dacite Work
- Completed Layer 2 cache refactor: `dacite.value.cache` namespace, `bind-store` macro, all tests green (commit `4b3d074`)
- Core tests now use `(binding [cache/*cache* (atom {})])` instead of `reset-store!` (commit `33d6983`)
- Updated Chapter 3 docs to match code: cache bridge, actual store implementations, future sections marked (commit `3d0d908`)
- Reviewed Layer 4 (Authorization) — docs describe ~30% implemented features
- Discussed Chapter 4 implementation design with Jonathan:
  - PUT protocol: client-driven DFS proof stream, server validates
  - `child-hashes` changed from sets to ordered vectors for deterministic DFS
  - Prover function as extension point (Layer 5 adds share roots to valid-roots set)
  - Auth stays pure — no LMDB/service knowledge
- Implemented: `validate-proof`, `verify-transition`, `apply-transition` (commit `412575b`)
- 378 tests, 1469 assertions, 0 failures
- Jonathan reviewing changes overnight

## Open Design Question
- `validate-proof` `:data` path trusts caller to verify hash matches value — should auth verify this itself?
